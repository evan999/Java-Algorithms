/*
        for(int i = 0; i < note.length; i++) {
            // Remove words that don't appear in magazine
            if(!magazineWords.containsKey(note[i])){
                System.out.println("No");
                return;
            }
            if(magazineWords.get(note[i]) == 0){
                System.out.println("No");
                return;
            }
            // Reduce count by 1 in 
            //magazineWords.set(note[i], magWords.get(note[i])-1)

        }
        */